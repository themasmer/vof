{
  "environment": "qa",
  "configurationSource": "environment variables or deployment secret manager",
  "requiredConfiguration": [
    "CLIENT_URL", "CORS_ORIGINS", "JWT_SECRET", "CSRF_SECRET",
    "DB_HOST", "DB_PORT", "DB_USER", "DB_PASSWORD", "DB_DATABASE",
    "SMTP_HOST", "SMTP_PORT", "SMTP_USER", "SMTP_PASS", "SMTP_FROM"
  ],
  "otp": { "allowedAttempts": 5, "validMinutes": 15 },
  "passwordReset": { "validMinutes": 60 },
  "session": { "validMinutes": 15, "refreshToken": 480 }
}
